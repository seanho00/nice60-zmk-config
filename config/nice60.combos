
#define COMBO(NAME, BINDINGS, KEYPOS, LAYERS) \
	combo_##NAME { \
		bindings = <BINDINGS>; \
		key-positions = <KEYPOS>; \
		layers = <LAYERS>; \
	}

compatible = "zmk,combos";
COMBO(grave, &kp GRAVE, 0 1, DEF_L);
COMBO(kbd_l, &mo KBD_L, 59 60, DEF_L LCK_L);

#undef COMBOS
